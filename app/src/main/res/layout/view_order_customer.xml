<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="order"
            type="com.tevioapp.vendor.data.Order" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:outlineSpotShadowColor="@color/card_shadow_color"
        app:cardBackgroundColor="@color/card_bg_dual"
        app:cardCornerRadius="@dimen/card_corner_radius_medium"
        app:cardElevation="@dimen/card_elevation_medium"
        app:strokeWidth="0dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/_10sdp">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <TextView
                    android:id="@+id/tv_customer_name"
                    style="@style/TextView.Medium"
                    android:text="@{order.customerInfo.fullName}"
                    android:textColor="@color/black"
                    android:textSize="@dimen/_12ssp"
                    android:textStyle="bold"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    tools:text="@string/order_id" />

                <TextView
                    style="@style/TextView.Medium"
                    android:text="@string/customer"
                    android:textColor="@color/black"
                    android:textSize="@dimen/_12ssp"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="parent" />
            </androidx.constraintlayout.widget.ConstraintLayout>

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/_10sdp"
                app:view_isVisible="@{order.dropInfo.dropOption!=null}">

                <View
                    android:layout_width="match_parent"
                    android:layout_height="@dimen/_1sdp"
                    android:background="@color/line_dual"
                    app:layout_constraintTop_toTopOf="parent" />

                <ImageView
                    android:id="@+id/iv_one"
                    android:layout_width="@dimen/_30sdp"
                    android:layout_height="@dimen/_30sdp"
                    android:layout_marginTop="@dimen/_10sdp"
                    android:importantForAccessibility="no"
                    android:src="@drawable/ic_placeholder_vertical"
                    app:image_place_holder="@{@drawable/ic_placeholder_vertical}"
                    app:image_url="@{order.dropInfo.dropOption.icon}"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent" />

                <androidx.constraintlayout.helper.widget.Flow
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="@dimen/_5sdp"
                    android:orientation="vertical"
                    app:constraint_referenced_ids="tv_opt1,tv_t2"
                    app:layout_constraintBottom_toBottomOf="@id/iv_one"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintStart_toEndOf="@id/iv_one"
                    app:layout_constraintTop_toTopOf="@id/iv_one" />

                <TextView
                    android:id="@+id/tv_opt1"
                    style="@style/TextView.Medium"
                    android:layout_width="0dp"
                    android:lines="1"
                    android:text="@{order.dropInfo.dropOption.title}"
                    android:textColor="@color/black"
                    android:textSize="@dimen/_13ssp"
                    android:textStyle="bold"
                    tools:text="@string/order_id" />

                <TextView
                    android:id="@+id/tv_t2"
                    style="@style/TextView.Regular"
                    android:layout_width="0dp"
                    android:lines="1"
                    android:text="@{order.dropInfo.dropOption.dropTypeName}"
                    android:textColor="@color/black"
                    android:textSize="@dimen/_11ssp"
                    tools:text="@string/order_id" />

            </androidx.constraintlayout.widget.ConstraintLayout>

            <com.google.android.material.card.MaterialCardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/_10sdp"
                app:cardBackgroundColor="@color/green_alpha_20"
                app:cardCornerRadius="@dimen/card_corner_radius_small"
                app:cardElevation="0dp"
                app:contentPadding="@dimen/_5sdp"
                app:strokeWidth="0dp"
                app:view_isVisible="@{order.dropInfo.hasDeliveryInstruction}">

                <TextView
                    android:id="@+id/tv_instruction"
                    style="@style/TextView.Regular"
                    android:layout_width="match_parent"
                    android:drawablePadding="@dimen/_5sdp"
                    android:maxLines="5"
                    android:text="@{order.dropInfo.deliveryInstructions}"
                    android:textColor="@color/black"
                    android:textSize="@dimen/_12ssp"
                    app:drawableStartCompat="@drawable/ic_note"
                    app:drawableTint="@color/dark_green"
                    tools:text="@string/order_id" />

            </com.google.android.material.card.MaterialCardView>

            <androidx.constraintlayout.widget.ConstraintLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/_10sdp">

                <androidx.constraintlayout.helper.widget.Flow
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    app:constraint_referenced_ids="btn_chat,btn_call"
                    app:flow_horizontalGap="@dimen/_10sdp"
                    app:layout_constraintTop_toTopOf="parent" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_chat"
                    style="@style/ButtonSolid.Icon"
                    android:layout_width="0dp"
                    android:text="@string/chat"
                    app:icon="@drawable/ic_message" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_call"
                    style="@style/ButtonStroke.Icon"
                    android:layout_width="0dp"
                    android:text="@string/call"
                    app:icon="@drawable/ic_phone_call_solid" />

            </androidx.constraintlayout.widget.ConstraintLayout>
        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

</layout>